services:
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: shipquery
      POSTGRES_PASSWORD: shipquery123
      POSTGRES_DB: shipquery
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
